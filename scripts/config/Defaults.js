define([
  "paper",
  "underscore"
], function(paper, _) {
  
  var Defaults = {
    tentativeStyle: {
      fillColor: "#a3e5f6",
      strokeColor: "#a3e5f6",
      strokeWidth: 0
    },
  
    committedStyle: {
      fillColor: "#0093b8",
      strokeColor: "#0093b8",
      strokeWidth: 0
    }    
  };

  return Defaults;
});
